import { StyleSheet } from "react-native"

export default StyleSheet.create({
    checkboxStyle:{
        height: 40,
        width: 40,
        backgroundColor: "#FFF",
        borderWidth: 2,
        borderColor: "#bcbcbc",
        borderRadius: 10,
        alignItems: 'center',
        justifyContent: 'center',
    }
})